rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // user docs must match their UID
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}
